#include "Container.h"

template <typename T>
Container<T>::Container() {
    cur_pos = 0;
    size = 200;
}

template <typename T>
void Container<T>::add(const T& elem) {

}

template <typename T>
void Container<T>::erase(const T& elem) {

}